import { LeaderHostDirective } from './leader-host.directive';

describe('LeaderHostDirective', () => {
  it('should create an instance', () => {
    const directive = new LeaderHostDirective();
    expect(directive).toBeTruthy();
  });
});
