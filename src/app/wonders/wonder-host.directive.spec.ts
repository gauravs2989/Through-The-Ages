import { WonderHostDirective } from './wonder-host.directive';

describe('WonderHostDirective', () => {
  it('should create an instance', () => {
    const directive = new WonderHostDirective();
    expect(directive).toBeTruthy();
  });
});
