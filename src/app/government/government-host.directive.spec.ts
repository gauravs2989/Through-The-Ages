import { GovernmentHostDirective } from './government-host.directive';

describe('GovernmentHostDirective', () => {
  it('should create an instance', () => {
    const directive = new GovernmentHostDirective();
    expect(directive).toBeTruthy();
  });
});
